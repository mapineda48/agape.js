export const MSGPACK_FIELD = "$";

// Content type for msgpack binary responses (Server Actions)
export const MSGPACK_CONTENT_TYPE = 'application/msgpack';

/**
 * Types
 */
export type Paths = Array<string | number>;