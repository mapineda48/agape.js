{
  "name": "agape.js",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test:run",
    "lint": "pnpm -r --if-present lint"
  },
  "packageManager": "pnpm@10.26.0"
}
